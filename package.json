{
  "name": "maida",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm ci --include=dev --prefix apps/web && npm run build --prefix apps/web && npm ci --include=dev --prefix apps/api && npm run build --prefix apps/api",
    "start": "npm run start:prod --prefix apps/api",
    "lint": "eslint apps/api/src apps/api/tests apps/api/scripts apps/web/src && prettier --check .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "eslint": "^10.7.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "prettier": "^3.9.5",
    "typescript-eslint": "^8.64.0"
  }
}
